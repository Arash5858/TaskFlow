# TaskFlow

TaskFlow is a lightweight command-line task manager written in Python.
It allows users to add, list, and complete daily tasks with minimal setup.

## Features
- Add new tasks
- View all tasks
- Mark tasks as completed
- Simple and readable codebase

## Requirements
- Python 3.8+

## Usage
Run the script and follow the on-screen menu:
```bash
python taskflow.py

---

## Code (taskflow.py)
```python
class TaskManager:
    def __init__(self):
        self.tasks = []

    def add_task(self, title):
        self.tasks.append({"title": title, "completed": False})

    def list_tasks(self):
        if not self.tasks:
            print("No tasks available.")
            return

        for index, task in enumerate(self.tasks, start=1):
            status = "Done" if task["completed"] else "Pending"
            print(f"{index}. {task['title']} [{status}]")

    def complete_task(self, index):
        try:
            self.tasks[index - 1]["completed"] = True
            print("Task marked as completed.")
        except IndexError:
            print("Invalid task number.")


def main():
    manager = TaskManager()

    while True:
        print("\nTaskFlow Menu")
        print("1. Add task")
        print("2. List tasks")
        print("3. Complete task")
        print("4. Exit")

        choice = input("Select an option: ")

    def list_tasks(self):
        if not self.tasks:
            print("No tasks available.")
            return
        if choice == "1":
            title = input("Enter task title: ")
            manager.add_task(title)
        elif choice == "2":
            manager.list_tasks()
        elif choice == "3":
            manager.list_tasks()
            number = int(input("Enter task number: "))
            manager.complete_task(number)
        elif choice == "4":
            break
        else:
            print("Invalid option.")


if __name__ == "__main__":
    main()
